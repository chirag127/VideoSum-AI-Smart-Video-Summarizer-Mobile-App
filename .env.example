# Environment variables for the VideoSum Mobile Platform

# API Base URL for the VideoSum backend service (where video processing/summarization requests are handled).
# This should point to your deployed backend instance.
EXPO_PUBLIC_API_BASE_URL="https://api.yourvideosum.com/v1"

# API Key for the AI service responsible for video summarization.
# Replace with your actual AI provider key (e.g., Google Gemini, OpenAI, or a custom ML service).
EXPO_PUBLIC_AI_SERVICE_API_KEY="your_ai_service_api_key_here"

# (Optional) Client ID for an authentication provider (e.g., Google, Apple, Auth0).
# If your app requires user authentication, configure this accordingly.
EXPO_PUBLIC_AUTH_CLIENT_ID="your_auth_client_id_here"

# (Optional) Sentry DSN for error monitoring and performance tracking.
# Uncomment and replace if using Sentry.
# EXPO_PUBLIC_SENTRY_DSN="https://examplepublickey@o0.ingest.sentry.io/0"

# (Optional) Firebase configuration details if Firebase is used for backend services (e.g., Auth, Firestore).
# Uncomment and replace with your project's Firebase config.
# EXPO_PUBLIC_FIREBASE_API_KEY="your_firebase_api_key"
# EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN="your_firebase_auth_domain"
# EXPO_PUBLIC_FIREBASE_PROJECT_ID="your_firebase_project_id"
# EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET="your_firebase_storage_bucket"
# EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID="your_firebase_messaging_sender_id"
# EXPO_PUBLIC_FIREBASE_APP_ID="your_firebase_app_id"
# EXPO_PUBLIC_FIREBASE_MEASUREMENT_ID="your_firebase_measurement_id"